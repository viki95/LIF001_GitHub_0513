const cloud = require('wx-server-sdk'); cloud.init(); exports.main = async (event, context) => { const axios = require('axios'); const API_KEY = "sk-0860827ca04347a6ba0b8c2b001a68fe"; const url = "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation"; const instance = axios.create({ timeout: 10000 }); try { const response = await instance.post(url, { model: "qwen-turbo", input: { messages: [ { role: "system", content: "你是大学生理财助手，回答简洁、实用、低风险，控制在300字以内" }, { role: "user", content: event.question } ] }, parameters: { result_format: "text" } }, { headers: { "Authorization": `Bearer ${API_KEY}`, "Content-Type": "application/json" } }); return { success: true, reply: response.data.output.text }; } catch (error) { console.error("请求失败：", error.response ? error.response.data : error.message); return { success: false, error: "调用失败" }; } };